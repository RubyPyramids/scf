# --- RPCs (explicit URLs or enable HELIUS_KEY below) ---
RPC_WS=wss://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY_HERE
RPC_PRIMARY=wss://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY_HERE
RPC_HTTP_PRIMARY=https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY_HERE
RPC_BACKUP=wss://api.mainnet-beta.solana.com
RPC_HTTP_BACKUP=https://api.mainnet-beta.solana.com
# Or derive from key (if code supports it):
# HELIUS_KEY=YOUR_API_KEY_HERE

# --- Jupiter ---
JUPITER_BASE=https://quote-api.jup.ag

# --- Database ---
DB_URL=postgresql://scf:scf@localhost:5432/scf

# --- Execution (live later) ---
EXEC_KEYPAIR_PATH=~/.config/solana/scf-keypair.json

# --- Logging ---
LOG_LEVEL=info

# --- Mode (runner CLI can override) ---
SCF_MODE=paper

# --- Detector thresholds (placeholders; tune) ---
SCF_VC_MAX=0.25
SCF_OFS_MAX=0.20
SCF_LT_MAX=0.30
SCF_WC_MIN=0.70
SCF_RQ_MAX=0.30

# --- Poll intervals ---
SCF_DETECTOR_POLL_SEC=5
SCF_EXECUTOR_POLL_SEC=5

# --- Program IDs (defaults ok) ---
RAYDIUM_AMM=675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8
RAYDIUM_CLMM=4hGdEStwqyqZkG2tZibsSDQ7SBy7xH2sVQ2QJVV5o4Ck
ORCA_AMM=9WwG7VJp49r4bgx1mVQqZKkGKuX3sX5Y3F9F6w8vG8bS
ORCA_WHIRL=whirLbMiicVq4SCVZxdrmB9otnE8u6VYzG9xH8Wc7so

# --- Exit engine (new) ---
SCF_TP_MULT=2.0
SCF_SL_MULT=0.30
SCF_EXIT_POLL_SEC=5
# Optional partial exits: comma-separated level:ratio pairs (e.g. 1.5:0.25,2.0:0.25)
#SCF_TP_PARTIAL=
#SCF_SL_PARTIAL=

# --- Live executor defaults (new) ---
SCF_TRADE_SIZE_SOL=0.05
SCF_SLIPPAGE_BPS=300